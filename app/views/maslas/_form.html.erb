<%= form_with(model: masla) do |form| %>
  <% if masla.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(masla.errors.count, "error") %> prohibited this masla from being saved:</h2>

      <ul>
        <% masla.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
